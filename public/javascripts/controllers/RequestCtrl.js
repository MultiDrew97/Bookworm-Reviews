angular.module('RequestCtrl', []).controller('RequestController', ($scope) => {
    // TODO: Implement the Review Request Page after figuring out how to manipulate angular pages
    $scope.tagline = 'Welcome to request section!';

    //TODO: Determine if the request will be its own page, or an addition to existing pages
});